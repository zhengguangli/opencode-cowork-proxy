# newsyslog.conf entry for opencode-cowork-proxy
# Install: sudo cp opencode-cowork-proxy.newsyslog.conf /etc/newsyslog.d/
# Test:     sudo newsyslog -nvv
# Rotate:   sudo newsyslog -F /usr/local/var/log/opencode-cowork-proxy.log
#
# logfile                    owner:group     mode  count  size   when  flags
/usr/local/var/log/opencode-cowork-proxy.log  lizhengguang:staff  644   7      10240  *     JB
