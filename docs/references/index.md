# References

> External reference materials, formatted for LLM readability.

This directory contains reference documentation from external sources (API specifications, protocol docs, upstream documentation) reformatted as LLM-friendly markdown.

## Purpose

External references are stored here so AI agents can reason about the project without needing web search access. All references should be in plain markdown with clear section headings.

## Contents

Currently empty. Candidates for inclusion:
- OpenAI Chat Completions API specification (relevant endpoints)
- Anthropic Messages API specification (relevant endpoints)
- Cloudflare Workers runtime API reference
- Hono framework API reference
- SSE specification
