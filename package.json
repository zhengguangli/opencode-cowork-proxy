{
  "name": "opencode-cowork-proxy",
  "version": "2.1.1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run scripts/build-entry.ts",
    "build:binary": "bun build --compile --outfile opencode-cowork-proxy scripts/build-entry.ts",
    "deploy": "npx wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  },
  "license": "MIT",
  "description": "Bidirectional AI API translation gateway for Cloudflare Workers. Anthropic↔OpenAI format bridge.",
  "dependencies": {
    "hono": "^4.12.17"
  }
}
